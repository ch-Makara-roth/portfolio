# Application Configuration
NODE_ENV=production
PORT=4000

# Database Configuration (using SQLite for now - can be changed to PostgreSQL later)
DATABASE_URL="file:./dev.db"

# JWT Configuration
JWT_SECRET=makara-portfolio-super-secret-jwt-key-2025-production
JWT_EXPIRES_IN=7d
